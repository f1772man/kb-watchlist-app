당신은 Antigravity 에이전트입니다. 아래 요구사항에 따라 **"관심종목 저장 + 실시간 시세 조회" 안드로이드 앱**을 이 빈 폴더에서 처음부터 끝까지 만들어 주세요. 기존에 만들어진 코드는 없다고 가정하고, 필요한 모든 것을 직접 설계·구현하세요. 구현 언어는 **Kotlin(안드로이드 네이티브)**로 지정하지만, 그 외 아키텍처·라이브러리·폴더 구조 등 세부 설계는 전적으로 당신의 판단에 맡깁니다. (에이전트 개발에서는 구현 언어 자체보다, 요구사항이 얼마나 정확히 전달되는지가 결과물의 품질을 좌우합니다. Kotlin이 낯설더라도 아래 스펙만 정확히 따르면 됩니다.)

## 목표

사용자가 국내 주식 종목을 검색해 관심종목으로 저장하고, 저장한 종목들의 현재가·등락률을 리스트에서 확인하며, 종목을 눌러 시가·고가·저가·거래량까지 담긴 상세 화면을 볼 수 있는 안드로이드 앱. 시세는 KB증권 Open API에서 가져옵니다.

## 전체 아키텍처

- **안드로이드 앱(Kotlin)**: Firebase Authentication으로 사용자를 구분하고, 자체 백엔드 API를 호출해 관심종목을 저장/조회하고 시세를 표시.
- **백엔드(Cloud Run)**: 앱과 KB증권 Open API 사이의 중계 서버. KB증권 앱키·시크릿을 안전하게 보관하고, 토큰을 캐싱하고, 사용자별 관심종목을 Firestore에 저장.
- **Firebase/Firestore**: 사용자 인증 + 관심종목 데이터 저장 + 토큰 캐시.

## 0단계 — 반드시 먼저 멈추고 사용자에게 확인할 것

아래는 본인 인증이 필요한 금융/계정 행위라 에이전트가 대신 할 수 없습니다. 진행 전에 사용자에게 아래를 확인하고, 없으면 사용자가 직접 처리하도록 안내한 뒤 기다리세요.

1. **KB증권 종합위탁계좌** 보유 여부. 없으면 https://openapi.kbsec.com 에서 계좌 개설부터 진행하도록 안내.
2. 위 포털에서 로그인 후 **"Open API 서비스 사용신청"(앱 등록)**을 완료해 **appKey / appSecret**을 발급받았는지. 발급받은 값을 사용자에게 받아서(코드에 직접 적지 말고, 뒤에서 Secret Manager에 저장) 이후 단계에 사용하세요.
3. Google 계정으로 Firebase/GCP 콘솔에 로그인되어 있는지. Cloud Run을 쓰려면 결제 계정 연결이 필요한데, 카드 등록 자체는 사용자가 직접 해야 합니다.

## 1단계 — 클라우드 인프라 구축

1. 새 Firebase 프로젝트 생성(같은 이름의 GCP 프로젝트가 자동 생성됨).
2. Firestore Database 생성 — Standard 모드, 리전 `asia-northeast3`(서울). 테스트 모드(30일 후 만료)에 의존하지 말고, 처음부터 "로그인한 본인 데이터만 읽고 쓸 수 있다"는 보안 규칙을 작성하세요.
3. Firebase Authentication 활성화 — 익명 로그인(Anonymous) 방식으로 사용자를 구분(간단한 데모용이므로 이메일 가입 절차는 생략해도 됩니다).
4. GCP 결제 계정 연결 확인, Cloud Run Admin API 활성화.
5. Secret Manager에 `kb-app-key`, `kb-app-secret` 이름으로 0단계에서 받은 값을 저장. 코드나 로그에는 절대 값이 노출되지 않게 하고, 이 프로젝트를 git으로 관리한다면 `.gitignore`로 시크릿 관련 파일(로컬 `.env` 등)이 커밋되지 않도록 하세요.
6. Cloud Run 서비스 계정에 `roles/secretmanager.secretAccessor`, `roles/datastore.user`, `roles/run.builder` 권한 부여.

## 2단계 — 백엔드(Cloud Run) 개발

REST API 서버를 만드세요(프레임워크는 자유). 아래 엔드포인트가 필요합니다.

- **시세 조회**: `GET /quote/{code}` — 내부적으로 KB증권 Open API를 호출해 현재가 등을 반환.
- **관심종목 조회/추가/삭제**: `GET /watchlist`, `POST /watchlist`, `DELETE /watchlist/{code}` — Firestore에 로그인한 사용자(uid)별로 저장. 클라이언트가 보낸 Firebase ID 토큰을 서버에서 검증해 uid를 추출하세요.

**KB증권 Open API 연동 스펙 (그대로 사용):**

- Base URL: `https://developer.kbsec.com:32484`
- 토큰 발급: `POST /oauth2/token`
  ```json
  { "dataHeader": {"ipAddr": "...", "macAddr": "..."},
    "dataBody": {"appKey": "{appKey}", "appSecret": "{appSecret}", "grantType": "client_credentials"} }
  ```
  응답의 `dataBody.access_token`(유효기간 `expires_in`초, 보통 86400)을 Firestore(`tokens/kb` 문서 등)에 저장해두고, 만료 10분 전까지는 재사용하세요. API 호출 시 헤더는 `Authorization: Bearer {access_token}`.
- 현재가 조회: `POST /api/v1/ivu10140`
  ```json
  { "dataHeader": {"ipAddr": "...", "macAddr": "..."},
    "dataBody": {"excg_clsf": "0", "shrt_cd": "005930"} }
  ```
  (`excg_clsf`: 0=통합/1=KRX/2=NXT, `shrt_cd`: 종목 6자리 코드)
  응답 `dataBody`에서 아래 필드를 꺼내 앱에 필요한 형태로 매핑하세요: `is_nm`(종목명), `now_prc`(현재가), `bdy_cmpr`(전일대비, 부호는 `bdy_cmpr_ccd`로 판정), `up_dwn_r_p2`(등락률%), `acml_vlm`(누적거래량), `opn_prc`/`hgh_prc`/`lw_prc`(시가/고가/저가).

**확인이 필요한 부분 (직접 조사 후 반영하세요):**
- KB증권 Open API에 모의투자(테스트) 환경이 별도로 있는지, 있다면 도메인이 무엇인지.
- 현재가 조회 실패 시 에러 응답 형식(어떤 필드로 실패를 판단하는지).
- `bdy_cmpr_ccd`(전일대비 구분코드)의 값 체계(어떤 코드가 상승/하락/보합인지).
- 요청의 `dataHeader.ipAddr`/`macAddr`가 실제 클라이언트 정보를 요구하는지, 예제처럼 더미 값(`127.0.0.1`/`00:00:00:00:00:00`)으로도 정상 동작하는지. Cloud Run 서버에는 실제 MAC 주소가 없으므로, 더미 값으로 안 되면 대안(서버 자체 IP 사용 등)을 문서에서 확인하세요.
이 네 가지는 https://openapi.kbsec.com/doc-intro (API문서) 에서 직접 확인해 정확히 반영하세요. 짐작으로 구현하지 마세요.

**유의사항:**
- 현재 KB증권 Open API는 WebSocket 실시간 시세를 지원하지 않습니다(REST 폴링만 가능). 베타 서비스라 유량제어(과도한 호출 시 일시 제한)가 있으니, 서버·앱 모두 짧은 주기로 과도하게 호출하지 않도록 설계하세요.
- 개발 완료 후 Cloud Run에 배포하되, 인증 없는 공개 접근은 허용하지 마세요(비공개 배포 + 클라이언트가 ID 토큰을 붙여 호출).

## 3단계 — 안드로이드(Kotlin) 앱 개발

아래 3개 화면을 만드세요(Google Stitch로 미리 설계한 화면 구성을 그대로 반영):

1. **홈 화면(관심종목 리스트)**: 상단 앱바 + 우측 검색 아이콘. 각 관심종목은 카드형으로, 왼쪽에 종목명·종목코드, 오른쪽에 현재가·등락률(%)을 크게 표시.
2. **종목 검색/추가 화면**: 상단 검색창, 입력하면 아래에 검색 결과 리스트, 각 항목 오른쪽에 "추가" 버튼(이미 추가된 종목은 체크 표시로 전환).
3. **종목 상세 화면**: 상단에 종목명·현재가·등락률을 크게, 그 아래 시가·고가·저가·거래량을 2x2 그리드 카드로 표시, 하단에 관심종목에서 제거하는 버튼.

**테마**: 차분한 네이비를 주조색, 골드 계열을 강조색으로 사용. 제목은 깔끔한 산세리프 폰트, 버튼은 모서리가 둥근 형태로 전체 통일. 상승 종목 숫자는 빨간색, 하락 종목 숫자는 파란색으로 앱 전체에서 일관되게 표시(한국 주식시장 관례).

**동작 방식**:
- 앱 실행 시 Firebase Authentication으로 익명 로그인.
- 홈 화면의 관심종목 시세는 일정 주기(예: 20~30초)로 백엔드 `/quote/{code}`를 폴링해 갱신(WebSocket 미지원이므로). 화면이 보이지 않을 때는 폴링을 멈추세요.
- 관심종목 추가/삭제는 백엔드 `/watchlist` API를 통해 처리.

**(참고) Stitch에 실제로 입력했던 원본 프롬프트** — 위 화면 구성은 아래 프롬프트로 Google Stitch에서 디자인한 결과를 요약한 것입니다. 화면 디테일이 애매하면 아래 원문을 우선 참고하세요.

```
[시작 프롬프트]
국내 주식 관심종목을 저장하고 실시간 시세를 확인하는 모바일 앱을 디자인해줘.
사용자는 종목을 검색해서 관심종목 리스트에 추가하고, 리스트 화면에서 각 종목의
현재가와 전일 대비 등락률을 한눈에 확인하며, 종목을 탭하면 시가·고가·저가·거래량까지
담긴 상세 화면으로 이동한다. 신뢰감 있고 차분한 금융 앱 분위기, 데이터 중심의
미니멀한 레이아웃, 상승은 빨간색 하락은 파란색으로 표시하는 한국 주식시장 색상
관례를 따르는 스타일로 만들어줘.

[화면별 정제 1 — 홈 화면]
홈 화면(관심종목 리스트)에 상단 앱바를 추가하고, 오른쪽에 종목 검색 아이콘을
배치해줘. 리스트의 각 항목은 카드 형태로, 왼쪽에 종목명과 종목코드, 오른쪽에
현재가와 등락률(%)을 크게 표시해줘.

[화면별 정제 2 — 종목 검색/추가 화면]
종목 검색 화면을 만들어줘. 상단에 검색창이 있고, 입력하면 아래에 종목 검색 결과
리스트가 나오며, 각 결과 항목 오른쪽에 '추가' 버튼이 있어. 이미 추가된 종목은
버튼 대신 체크 표시로 바꿔줘.

[화면별 정제 3 — 종목 상세 화면]
종목 상세 화면을 만들어줘. 상단에 종목명과 현재가, 등락률을 크게 표시하고, 그
아래 시가·고가·저가·거래량을 2x2 그리드 카드로 배치해줘. 화면 하단에는
관심종목에서 제거할 수 있는 버튼을 추가해줘.

[테마 통일 프롬프트]
전체 화면의 테마를 통일해줘. 주요 브랜드 컬러는 차분한 네이비, 강조색은 골드
계열로 하고, 제목은 깔끔한 산세리프 폰트를 사용해줘. 모든 버튼은 모서리가 둥근
형태로 통일하고, 상승 종목의 숫자는 빨간색, 하락 종목의 숫자는 파란색으로 앱
전체에서 일관되게 적용해줘.
```

## 4단계 — 검증

- 에뮬레이터(또는 실기기)에서 앱을 실행해, 종목 검색 → 관심종목 추가 → 홈 화면에서 시세 표시 → 상세 화면 진입까지 전체 흐름이 동작하는지 확인하세요.
- Firestore 콘솔에서 `tokens/kb` 문서에 토큰이 저장·재사용되는지, 사용자별 관심종목 컬렉션이 정상적으로 쌓이는지 확인하세요.
- 진행 중 막히는 부분이 있으면(특히 0단계의 계좌/키 관련 사항) 임의로 추측해 넘어가지 말고 사용자에게 물어보세요.
